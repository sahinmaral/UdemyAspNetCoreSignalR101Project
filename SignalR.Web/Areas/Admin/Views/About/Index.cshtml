@model List<ResultAboutViewModel>

@{
    ViewBag.Title = "Hakkında";
    Layout = "_AdminLayout";
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Hakkında işlemleri</h4>
        <div class="row">
            <div class="col-md">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">
                            Hakkında listesi
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="card-sub">
                            <p>Hakkında ile ilgili işlemleri aşağıda gerçekleştirebilirsiniz.</p>
                            <a href="/Admin/About/Create" class="btn btn-primary">Yeni hakkında girişi</a>
                        </div>
                        @if (Model.Count == 0)
                        {
                            <div class="alert alert-info">
                                Herhangi bir hakkında yok.
                            </div>
                        }
                        else
                        {
                            <table class="table table-striped mt-3">
                                <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Başlık</th>
                                    <th scope="col">İşlemler</th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var about in Model)
                                {
                                    <tr>
                                        <td>@about.Id</td>
                                        <td>@about.Title</td>
                                        <td>
                                            <a class="btn btn-outline-success" href="/Admin/About/Update/@about.Id">Güncelle</a>
                                            <a class="btn btn-outline-danger" href="/Admin/About/Delete/@about.Id">Sil</a>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        }
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>